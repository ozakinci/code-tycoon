# Code Tycoon

An idle/clicker game where you write code and hire a team to build a software empire.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
3. Open http://localhost:3000
